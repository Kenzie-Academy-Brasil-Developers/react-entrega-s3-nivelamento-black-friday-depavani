const PromotionGenerator = ({ randomSort }) => {
  return (
    <>
      <button className="initialButton" onClick={randomSort}>
        Gerar promoção
      </button>
    </>
  );
};

export default PromotionGenerator;
